const root=document.documentElement,header=document.querySelector('.site-header'),nav=document.querySelector('.main-nav'),toggle=document.querySelector('.menu-toggle'),topBtn=document.querySelector('.back-top');
document.getElementById('year').textContent=new Date().getFullYear();
toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open);toggle.setAttribute('aria-label',open?'Cerrar menú':'Abrir menú')});
document.querySelectorAll('.main-nav a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')}));
addEventListener('scroll',()=>{header.classList.toggle('scrolled',scrollY>20);topBtn.classList.toggle('show',scrollY>500)},{passive:true});
topBtn.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
document.querySelectorAll('[data-set-theme]').forEach(btn=>btn.addEventListener('click',()=>{root.dataset.theme=btn.dataset.setTheme;localStorage.setItem('servicom-theme',btn.dataset.setTheme);document.querySelectorAll('[data-set-theme]').forEach(b=>b.classList.toggle('active',b===btn))}));
const saved=localStorage.getItem('servicom-theme');if(saved){root.dataset.theme=saved;document.querySelectorAll('[data-set-theme]').forEach(b=>b.classList.toggle('active',b.dataset.setTheme===saved))}
const revealObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');revealObserver.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));
const animateCount=el=>{const end=+el.dataset.count,duration=1400,start=performance.now();const tick=now=>{const p=Math.min((now-start)/duration,1),value=Math.round(end*(1-Math.pow(1-p,3)));el.textContent=(el.dataset.prefix||'')+value+(el.dataset.suffix||'');if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)};
const countObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.querySelectorAll('[data-count]').forEach(animateCount);countObserver.unobserve(e.target)}}),{threshold:.5});countObserver.observe(document.querySelector('.credibility'));
const form=document.querySelector('.contact-form');form.addEventListener('submit',e=>{e.preventDefault();let valid=true;form.querySelectorAll('[required]').forEach(field=>{const ok=field.type==='checkbox'?field.checked:field.value.trim()!==''&&(!field.validity||field.validity.valid);field.classList.toggle('invalid',!ok);valid=valid&&ok});const status=form.querySelector('.form-status');if(!valid){status.textContent='Por favor, completa correctamente los campos obligatorios.';status.style.color='#b42318';return}status.textContent='¡Gracias! Tu solicitud fue recibida. Te contactaremos a la brevedad.';status.style.color='#08783e';form.reset()});form.addEventListener('input',e=>e.target.classList.remove('invalid'));
